<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="org.darenom.visualtralslator.MainActivity">

    <!-- The main content view -->
    <RelativeLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:drawableTop="@drawable/loader"
            android:background="@color/colorPrimary"/>

        <include
            android:id="@+id/main_layout_preview"
            layout="@layout/layout_preview" />

        <include
            android:id="@+id/main_layout_translate"
            layout="@layout/layaout_translate" />

        <include
            android:id="@+id/actions"
            layout="@layout/layout_actions" />

    </RelativeLayout>

    <!-- The navigation drawer -->
    <RelativeLayout
        android:id="@+id/left_drawer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@android:color/transparent">

        <Button
            android:id="@+id/reload"
            android:layout_width="@dimen/icon_update"
            android:layout_height="@dimen/icon_update"
            android:background="@drawable/ic_cached_black_24dp"
            android:layout_centerInParent="true" />

    </RelativeLayout>
</android.support.v4.widget.DrawerLayout>